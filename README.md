# PlayerokBot - Автоматический бот для продвижения товаров

Неофициальный бот для автоматического продвижения товаров на торговой площадке Playerok. Бот периодически проверяет ваши товары и продвигает их до премиум статуса, если они соответствуют заданным критериям времени публикации и фильтрам.

## Особенности

- 🚀 **Автоматическое продвижение**: Автоматически продвигает товары на премиум статус
- ⏲️ **Настраиваемые интервалы**: Гибкие настройки времени между проверками и продвижениями
- 🎯 **Точные фильтры**: Продвижение только нужных товаров по имени
- 🛡️ **Защита от блокировки**: Обработка rate limiting и повторные попытки
- 📊 **Подробная статистика**: Отчеты о количестве проверенных и продвинутых товаров
- ❄️ **Низкая нагрузка**: Задержки между запросами для избежания блокировок

## Как это работает

1. Бот подключается к вашему аккаунту через API токен
2. Периодически проверяет список товаров со статусом APPROVED
3. Фильтрует товары по заданным критериям (время публикации, имя товара)
4. Продвигает подходящие товары до премиум статуса
5. Выводит подробные логи операций

## Требования

- Python 3.8+
- Учетная запись на [Playerok](https://playerok.com)

## Установка

1. Скачайте или склонируйте репозиторий:
   ```bash
   git clone https://github.com/almaz4444/PlayerokBot.git
   cd PlayerokBot
   ```

2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

## Настройка

1. Скопируйте пример конфигурации:
   ```bash
   cp config.example.yaml config.yaml
   ```

2. Откройте `config.yaml` и заполните:
   - **token**: Ваш API токен Playerok (можно получить из браузера)
   - **username**: Ваше имя пользователя на Playerok
   - **user_agent**: User-Agent браузера (можно оставить по умолчанию)
   - **Интервалы**: Настройте временные задержки
   - **Фильтры**: Укажите какие товары продвигать

### Получение токена Playerok

1. Зайдите на [playerok.com](https://playerok.com)
2. Авторизуйтесь в аккаунте
3. Откройте инструменты разработчика (F12)
4. Перейдите во вкладку "Application" -> "Local Storage" -> "https://playerok.com"
5. Найдите ключ "token" - это ваш API токен

## Запуск

```bash
python main.py
```

Бот запустится и начнет работу согласно настройкам. Вы увидите баннер с текущей конфигурацией и статистику продвижения.

## Конфигурация

| Настройка | Описание | Значение по умолчанию |
|-----------|----------|----------------------|
| `credentials.token` | API токен Playerok | - |
| `credentials.username` | Имя пользователя | - |
| `intervals.update_interval_min/max` | Интервал между проверками (секунды) | 1800 |
| `intervals.delay_between_promos_min/max` | Задержка между продвижениями (секунды) | 1-3 |
| `intervals.promo_threshold` | Минимальное время после публикации (минуты) | 30 |
| `settings.verbose` | Подробный вывод | true |
| `settings.item_status` | Статус товаров для продвижения (APPROVED/SOLD) | APPROVED |
| `filters.include_names` | Продвигать только эти названия | [] |
| `filters.exclude_names` | Исключить эти названия | [] |
| `filters.duplicates_limit` | Лимит дубликатов на имя (-1 = без лимита) | -1 |

## Сборка исполняемого файла

Для создания standalone исполняемого файла:

```bash
pip install pyinstaller
pyinstaller build.spec
```

Исполняемый файл появится в папке `dist/PlayerokBot.exe`

## Безопасность

⚠️ **Важно:**
- Не передавайте свой токен и конфигурацию третьим лицам
- Токен предоставляет полный доступ к вашему аккаунту
- Используйте фильтры имен для контроля продвижения нужных товаров
- Следите за балансом - продвижение платное

## Логика продвижения

Бот продвигает товары которые:
- Опубликованы более N минут назад (`promo_threshold`)
- Соответствуют фильтрам имен (include/exclude)
- Еще не продвинуты в этой сессии (если лимит дубликатов)
- Имеют доступный премиум статус

## Поддержка

Обратитесь к документации PlayerokAPI: https://playerokapi.readthedocs.io/
Или создайте Issue в репозитории.

## Лицензия

Проект предоставляется "как есть", без каких-либо гарантий. Используйте на свой страх и риск.
